SAVE YOUR 4 POSTER IMAGES IN THIS FOLDER. USE THESE NAMES
(extension can be EITHER .jpg OR .png — both detected automatically):

  1. yoga          →  International Day of Yoga 2026 poster
  2. swachhta      →  Swachhta Pakhwada 2026 poster
  3. ijtema        →  All Tripura Arkan Ijtema 2025 poster
  4. islamic-camp  →  Summer Islamic Camp 2025 poster

  e.g. yoga.jpg OR yoga.png both work; ijtema.png OR ijtema.jpg both work.

YOUR LOGO (for the nav bar + footer):

  logo.png  →  your circular DesignHub logo

  IMPORTANT: crop it TIGHT to the circle (no white border around it) so it
  fills the frame. A square crop where the colorful ring touches all 4 edges
  looks best. The site adds its own spinning multi-color halo behind it.


OPTIONAL — for the BEST website screenshots, save your own here too:

  5. web-healthcare.jpg  →  screenshot of Northeast Healthcare site
  6. web-laxmi.jpg       →  screenshot of Laxmi Candle Store site

If these exist, they are used. If not, the site auto-generates a clean
screenshot via Microlink (waits for the full page to load). Either way
the card sits inside a nice browser-window frame.

How to take a great screenshot: open the site in Chrome, press F11 for
fullscreen (or just maximize), then use Windows Snipping Tool (Win+Shift+S)
to grab the top portion of the page. Save it with the name above.

Tips:
- .jpg or .png both work (if you use .png, change the filename in index.html).
- If a file is missing, the card automatically shows a styled fallback — it will never look broken.
